# 财报助手交接说明

更新日期：2026-08-06

仓库：<https://github.com/kaguravicky-web/earnings>

线上页面：<https://kaguravicky-web.github.io/earnings/>

## 1. 产品目标

这不是新闻摘抄页，也不是交易信号工具。目标是回答四个问题：

1. 公司本季经营到底发生了什么？
2. 数字、管理层解释和股价反应是否一致？
3. 市场正在形成什么叙事，哪些证据支持或反驳它？
4. 上一季度的预测是否兑现，下一季度应如何裁决？

用户主要在手机上阅读，因此首屏必须紧凑、数字一眼可读、长文可展开和收起。

## 2. 当前状态

- 纯静态 GitHub Pages，无后端、数据库和构建流程。
- 所有页面代码和数据目前都在 `index.html`。
- 页面包含：分析记录、叙事主线、并购跟踪、评分框架。
- 分析记录支持板块筛选、结论筛选、搜索、Revenue Growth 排序和收入指引排序。
- 重要财报可标记为“板块扩展”；当前示例为 APP 与 PLTR。
- 记录按板块展示，并保留同一公司未来季度纵向比较所需的数据结构。

## 3. 不可违背的编辑原则

- 全程简体中文，白底黑字，移动端优先。
- 标题固定为“财报助手”，不要再加英文 eyebrow 或“深度分析档案”等副标题。
- 不提供买入、卖出、止损、仓位和目标价。
- 来源放在正文末尾作为注脚式链接，不做醒目的来源标签墙。
- 财报数字使用紧凑快照，不把“实际值、预期、同比、指引”写成长句。
- 管理层发言重点只强制覆盖大盘股、权重股和能影响板块的公司。
- X 只能作为市场叙事和线索，不能替代公司公告、SEC、IR 或电话会原文。
- 评分要吝啬。优秀数字不等于优秀股价结果，也不等于叙事可持续。
- 不使用某一只股票作为永久 benchmark；采用跨季度、跨公司的分位标准。
- 同一叙事只保留一个展开区，不要拆成“摘要”和“详情”两套重复内容。

## 4. 评分框架

总分 100：

- 财报质量 40：收入、利润、关键 KPI、指引和连续季度趋势。
- 股价反应 35：即时反应、持续性、相对板块表现，以及价格与数字的分歧。
- 持续性质量 25：增长来源、现金流、护城河、订单可见度、一次性因素与反叙事。

分数不是投资建议。90 分以上应非常少见；数字很好但股价很差时，必须明确扣除股价反应分，并解释市场疑虑。

## 5. `index.html` 数据位置

- `NARRATIVES`：市场主线。
- `RECORDS`：财报与事件记录；新记录插入 `/* __RECORDS_INSERT_POINT__ */` 后。
- `DEALS`：并购跟踪。
- `SECTOR_META` / `TICKER_SECTOR`：板块映射。
- `COMPARABLES`：排序所需的 Revenue Growth 与收入指引增速补充值。
- `metricCards()`：财报数字快照。
- `extendedHtml()`：板块扩展分析。
- `render()`：筛选、排序和主列表渲染。

`RECORDS` 是 JavaScript 对象数组，不是严格 JSON。`report` 使用反引号模板字符串。

## 6. 板块扩展的触发标准

不要给每家公司机械增加扩展。至少满足以下一项：

- 财报改变了整个行业的增长或估值预期；
- 公司位于关键瓶颈，数据可传导到多家上下游公司；
- 管理层提出的新叙事同时被订单、收入、现金流和股价验证；
- 财报造成明显板块联动，需要区分基本面受益与情绪高 Beta。

扩展必须包含：核心矛盾、增长路径、管理层解释、外部证据或反叙事、分层板块传导、下一季度裁决清单。关联公司不能只是 ticker 列表，必须说明传导机制和需要验证的指标。

## 7. 研究与信源优先级

1. SEC 文件、公司 IR 财报稿、股东信和正式指引。
2. 官方业绩会文字稿或视频。
3. 权威财经媒体的市场反应和一致预期。
4. 管理层采访。
5. X、第三方流量、下载、信用卡或广告数据。

数字冲突时，以官方口径为实际值；一致预期必须注明来源或写“约”。第三方数据必须写清它是在验证还是挑战管理层说法。

## 8. 发布前检查

在仓库根目录执行：

```powershell
node -e "const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const m=s.match(/<script>([\s\S]*?)<\/script>/);new Function(m[1]);console.log('script ok')"
git diff --check
git status -sb
```

还要在浏览器检查：记录数量、APP/PLTR 扩展区、展开/收起、板块筛选、两个排序、移动端横向溢出。

当前协作习惯是验证后直接推送 `main`，因为用户要求自动更新网站。若后续维护者希望改用 PR，先与用户确认。

## 9. 已知技术债

- 单文件已较大，后续应把数据、渲染代码和样式拆开，但必须保持 GitHub Pages 零构建可用。
- 记录正文与 `reports/` Markdown 目前需要人工同步。
- 同一公司跨季度的对比 UI 还可以进一步强化。
- 财报后 5 日、20 日复盘尚未自动化。
- X 与视频没有稳定 API，需要逐次检索和人工核验。

## 10. 安全边界

仓库是公开的。不要提交 token、Cookie、个人隐私、心理侧写、本地私人项目路径或其他项目的实现细节。
