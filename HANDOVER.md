# 交接文档：财报深度分析助手

**交接方：** Claude (Claude Code / claude.ai)
**接手方：** Codex
**交接日期：** 2026-07-15
**仓库：** https://github.com/kaguravicky-web/earnings （public）
**用户：** Vic — 美股活跃交易者（本文档终端用户，非本仓库作者）

---

## 0. 先读这段：这个项目现在处于什么状态

这不是一个成熟的软件系统，是一个**在对话里跑了大约10天、逐步演化出来的分析流程**，大部分"实现"其实是写在这份系统提示词里的规则，由 LLM 每次现场执行，而不是代码。目前仓库里只有一个静态 HTML 归档页——**没有后端、没有数据库、没有定时任务、没有评分计算代码**。评分是每次分析时由 LLM 手动套用规则推算出来的数字，不是某个脚本算出来的。这一点非常重要，会反复在下文出现。

这个项目经历过一次方向性转变：最初叫"财报动能交易助手"，给买卖建议（🚀立即追/🔴不碰这种），2026-07-08 起 Vic 明确要求转向"只做基本面深度分析，不做交易建议"，原因是"财报是滞后数据，市场的解读才是信息增量最大的部分"。**这次转向没有做完美——细节见第6节，Codex接手时最容易在这里踩坑（把旧的交易框架和新的分析框架搞混）。**

Vic 名下还有其他与本仓库完全独立、不公开的项目，其中一个也用到 LongBridge 相关工具链，遇到过一些类似的技术问题（例如下文提到的历史K线接口故障）。**那些项目的内部实现细节不属于本仓库范围，本文档不引用其具体文件/脚本**，只在下文相关处提及"存在一个已验证可行的技术方案"这个结论本身，供 Codex 参考验证思路。本交接文档只覆盖"财报深度分析助手"（这个 `earnings` 仓库）。

---

## 1. 核心规则与提示词

### 1.1 系统提示词文件

- [`prompts/系统提示词_v2_基本面分析版.md`](prompts/系统提示词_v2_基本面分析版.md) —— **当前生效版本**。这是 Vic 原始维护在本地 `Downloads\财报动能交易助手_系统提示词.md` 的文件，本次交接原样复制进仓库。**本地文件和仓库副本目前是两份独立拷贝，没有同步机制**——如果 Vic 之后在本地改了系统提示词，仓库这份不会自动更新。这是需要 Vic 决定的事项：以后谁是"唯一真相源"？建议以后改动都先改仓库里这份，本地文件改成一个符号链接或者直接不再维护本地版本。
- [`prompts/系统提示词_v1_交易信号版_已废弃.md`](prompts/系统提示词_v1_交易信号版_已废弃.md) —— 2026-07-08 之前的旧版（给买卖建议、Peel减仓计划那版）。已废弃，留作历史参考，**不要用这版的框架**。

系统提示词文件里已经包含：
- 角色定义 + 数据规则
- 核心哲学（财报=滞后数据，市场解读=领先信息）
- 模式一（财报深度分析）完整评分框架 + 标准输出格式模板
- 模式二（事件产业链分析）完整触发条件 + 标准输出格式模板
- 补充规则（价格优先级、模式判断、数据缺失处理等）

**"标准提问模板"不存在。** Vic 的实际用法是自然语言对话——"GS和IBM财报出了"这种一句话，没有固定的填空式问题模板。这是对话驱动的产品，不是表单。如果 Codex 打算做成更结构化的输入（比如一个 CLI 参数或表单），这是一个可以主动补的东西，但目前没有。

**"T+2、T+3、T+5 复盘模板"不存在，这是本次交接请求里被高估的一项。** 目前只有两个自由文本字段（`vic_notes` 和 `outcome`，见第4.4节 RECORDS 结构），Vic 提出后续观点或复盘结果时由 LLM 手动补进这两个字段，没有固定格式、没有强制在 T+2/T+3/T+5 这些具体时间点提醒去做复盘。每份分析报告结尾的"下一个验证点"部分（① ② ③ 列表）实际上起到了"复盘检查清单"的作用，但这是分析报告的一部分，不是独立的复盘模板。**建议 Codex 如果要做复盘模板，可以把"下一个验证点"列表结构化，变成一个可勾选/可填结果的检查清单，绑定到对应的 `date + ticker`，但这需要新设计，不是现成的。**

### 1.2 100分评分体系——固定规则 vs 主观判断

**先说结论：这套"评分体系"里，大约一半的子项有明确、可复现的计分规则；另一半只给了满分分值，没给中间档的判定标准，完全靠分析者（目前是我，以后是 Codex）现场判断打几分。** 这不是我藏着掖着，是系统提示词原文就是这样写的——很多地方只写"XX（8分）"没有写"什么情况下打几分"。下面逐项标注：

#### 财报质量（40分）

| 子项 | 分值 | 规则明确程度 |
|---|---|---|
| EPS vs 预期 | 8 | ❌ 只说"超出幅度"，没有分档。我这次手算时用的是"完全按超出比例主观打分"，没有一个可复用的公式 |
| 营收 vs 预期 | 7 | ❌ 同上 |
| 指引 vs 预期 | 10 | ⚠️ 部分明确——**miss=0分是硬规则**，但beat的情况下打几分没有分档 |
| 历史惊喜对比 | 10 | ✅ **完全明确**，见下方专门说明，是整个框架里唯一有完整数值表的部分 |
| 盈利质量 | 5 | ⚠️ 半明确——"+2改善/0持平/-1恶化"这种三态给了具体分值，但"改善/持平/恶化"怎么判定是主观的 |

**历史惊喜对比（10分）的完整规则**（这是唯一可以写成确定性代码的部分）：

1. 取近4个季度的 EPS 实际值和预期值，算出每季度的惊喜幅度 `surprise = (实际-预期)/|预期| × 100%`
2. 算本季度惊喜相对上季度惊喜的边际变化：`rel = (本季surprise - 上季surprise) / |上季surprise| × 100%`
3. 按 `rel` 查表：`>50% → 10分`，`1~50% → 8分`，`-10%~10%（持平） → 5分`，`-30%~-1%（收窄） → 2分`，`<-30% → 0分`
4. **连续两季收窄 → 0分再倒扣2分（即-2分）；连续三季收窄 → 0分并标注🔻恶化确认**（这条本次没有明确说是不是"强制否决"，v1旧版里连续三季收窄是硬性否决项，v2只写"标注恶化确认"，语气弱化了，但没写清楚是否等同于自动判定为🔻这个最终结论——**这是一个没问清楚的模糊点，建议下次问Vic**）
5. **特例：如果上季度是miss（surprise≤0）、本季度转正（surprise>0），直接按"扩大>50%"处理，给满分10分**——这是"扭亏/拐点"特例，写在系统提示词里的明文规则

**这次实测踩到的、系统提示词完全没覆盖的边界情况：**
- **连续两季都是 miss（不是"收窄的beat"，是真的连续两次不及预期）——规则完全没写这种情况怎么算。** 我这次处理 IBM 时，IBM 是"连续8季度beat后单季转为miss"，不属于"连续miss"，所以没真正踩到这个坑，但可以预见如果一家公司连续两季miss，现有的"扩大/收窄"框架（专门为beat流的强弱设计）会失效，需要新judgment。
- **"由beat转为miss"这种单季反转，rel计算出来会是一个巨大的负数（IBM这次是-148%），直接落进"<-30%"这个桶里，给0分——数值上凑巧成立，但这不是框架设计者刻意想到的场景，只是数学上恰好落进了对的桶。** 能用，但严格说是巧合而不是设计。

#### 市场解读（35分）——**全框架主观程度最高的部分，也是权重第二大的部分**

| 子项 | 分值 | 规则明确程度 |
|---|---|---|
| 价格反应 | 12 | ❌ **完全没有数值规则**，只说"与beat/miss幅度相称为满分基准"，相称不相称完全靠判断。这是本次分析里我给分最主观的一项（IBM我打了0/12，理由是"跌25%但EPS只miss2.7%"，但"相称/不相称"到底该怎么量化打分，没有公式） |
| 持续性 | 8 | ✅ 明确——次日方向±4/-2/0，2-3日守住/回吐±4/0，两项相加=8满分 |
| 成交量确认 | 5 | ✅ 明确——>3倍=5，2-3倍=3，<2倍=1，缩量=0 |
| 聪明钱信号 | 10 | ⚠️ 半明确——升评/下调、IV方向、机构异动、同行联动都给了具体加分值，但"同行联动算不算数""数据缺失时是否要往下扣"没写清楚 |

#### 基本面深度（25分）——**完全没有任何分档规则，纯主观**

核心驱动（8）/Backlog（6）/管理层语气（5）/竞争格局（6）——系统提示词原文只给了这四个类目和满分分值，**一个具体的加减分规则都没有**。这次我给GS打 21/25（7+5+5+4）、给IBM打 10/25（3+2+3+2），这些具体数字完全是我当场读完材料后的主观判断，没有任何可复现的公式支撑。**如果换一个人（或者Codex）来做同一份分析，大概率会打出不同的具体分数，只是方向（相对高低）应该是一致的。**

#### 四象限判定（共振确认/市场存疑/预期重定价/恶化确认）

规则原文是"数字强/弱 × 市场买/卖"的2×2表格，但**"强/弱"和"买/卖"本身没有量化阈值**。我这次实际操作是：
- "强/弱"≈ 财报质量维度是beat还是miss（不是看总分，是看第一维度的方向）
- "买/卖"≈ 当天价格方向是正是负（不看具体百分比门槛）

这是我自己的操作惯例，不是系统提示词写明的规则，**Codex 应该沿用同一套操作惯例以保持一致性，除非 Vic 明确要求改**。

#### 红旗规则——**这部分是全框架里最明确的**，四条都是硬阈值：
- P/S>50x 或 Forward PE>200x
- 内部人90天卖出>买入10倍 且 股价历史高位
- 证券诉讼/监管调查进行中
- 单季FCF深度为负 且 债务>市值

**边界案例：GS这次内部人90天是"0次买入、148次卖出"，买入是0，"卖出>买入10倍"这个比值在分母为0时无定义。** 我的处理是：不当作红旗触发（因为大型金融机构高管零买入、持续卖出是常态——薪酬变现），只在报告里提及但不打🚩标签。这是我的判断，不是规则明文规定的除零处理方式。

### 1.3 已知的评分边界案例汇总（防止Codex重新踩坑）

1. 财报质量维度理论上可以算出**负分**（IBM这次原始加总是-1/40）。系统提示词没说负分怎么显示，我自己决定**在HTML归档和报告文本里 clamp 到 0，但在报告正文里注明"实际计算为负值"**。这是我的展示惯例，不是规则。
2. 银行/金融股**没有常规单季度EPS指引**，"指引vs预期（10分）"这个子项对银行股不适用。我这次的处理是**用管理层前瞻性表态（backlog、行业展望）当代理指标**，打了接近满分。这是我为了让框架能用而做的临时适配，系统提示词完全没提到"某些行业不适用某个子项时怎么办"这个问题，是个真实的框架缺口。
3. "同业普涨稀释个股alpha"的问题（GS这次和JPM/WFC同日报喜）——系统提示词的"聪明钱信号"和"价格反应"两项都没有内建"扣掉板块beta"的机制，我是手动在报告文字里加了一段定性说明并且扣了分，**但没有一个数值公式来精确量化"这次上涨里有多少是个股专属"**。
4. IBM这次的"抢跑式预警"（正式财报会是7/22，但7/14先发了preliminary结果警示miss）——**系统提示词里"指引miss"这条规则默认假设是"随财报一起发布的下季度指引"，没有覆盖"财报本身以非常规方式提前预告"这种情况。** 我把它当作等同于指引miss处理（标注⚠️更严重的信号），这是我的解读，不是规则明文规定。

---

## 2. 数据来源与核验方法

**先说最重要的一句话：除了实时价格，这套流程里几乎所有数据都来自通用网页搜索（WebSearch），没有任何专门的财报数据API或订阅服务被接入。** 财报助手目前是"每次现查现搜"，数据质量取决于当天搜索引擎能搜到什么，**没有缓存、没有去重、没有固定数据源清单**。完整清单见 [`DATA_SOURCES.md`](DATA_SOURCES.md)（含每类数据的获取方式、常见失败、替代来源、冲突时如何取舍）。

---

## 3. 依赖 Claude 特定能力的地方（Codex 接手前必须逐条确认替代方案）

这是本次交接里最关键的一节，请认真读。

1. **LongBridge MCP `quote-realtime-info`**：Claude Code 的 MCP 连接器，用来拿实时价格（含盘前/盘中/盘后独立字段）。这是整套流程里**唯一一个真正结构化、可靠的数据源**。Codex 如果没有同一个 MCP 连接器，需要找替代方案——已知一条可行路线：直接用官方 `longport` Python SDK + 用户自己的 LongBridge API 凭证（环境变量形式，例如 `LONGPORT_APP_KEY`/`LONGPORT_APP_SECRET`/`LONGPORT_ACCESS_TOKEN`），绕开 MCP 包装层直接调用。**这条路径本次交接没有移植进本项目，只是记录存在这个已验证可行的方案。**
2. **LongBridge MCP `quote-history-candlesticks`（历史K线/成交量均值）：确认损坏。** 无论传什么参数都报同一个 Zod validation 错误（列出1-18所有period字面量），本次会话重新测试过依然复现，不是用法问题。已知修复方式同上——绕过 MCP，直接用官方 `longport` SDK 调用。本项目至今没有用过这条修复路径，所有"放量倍数"描述目前都是从新闻报道里抄的定性描述（"创纪录成交日"），不是真算出来的倍数。**这是一个已知有解法、建议 Codex 优先落地的修复项。**
3. **WebSearch 工具**：Claude Code 内置的网页搜索工具，自动返回带链接的结果并强制要求列出来源。这是整个"网页数据补充"环节的唯一渠道。Codex 需要一个功能等价的搜索能力（无所谓具体实现，只要能搜到财经新闻/SEC文件/分析师数据）。
4. **Claude Code 的自动记忆系统（`~/.claude/projects/.../memory/*.md`）**：这套系统会在对话间自动积累"用户是谁、偏好什么、项目进展到哪"这些知识，本交接文档里第1、5、6节的很多内容其实就是从这套记忆系统里摘出来重写的。**Codex 大概率没有等价机制**，这正是本次要做"交接包"的原因——把本来活在 Claude 记忆系统里、别处看不到的知识，一次性写成本仓库里的文件。**这意味着：以后 Codex 自己积累的新知识，也需要主动写回仓库文档，不能指望有一个自动记忆层帮它做这件事。**
5. **归档动作依赖本地文件系统 + Bash + git，且分两步、手动衔接：**
   - HTML归档文件的"真身"目前在 Vic 本地 Windows 机器上：`D:\交易为生\output\财报交易助手.html`
   - 推送到 GitHub 仓库是**本次会话里手动做的一次性操作**：clone 仓库到一个临时 scratch 目录 → `cp` 本地文件进去 → `git commit` → `git push`。**这个流程没有自动化，下次 Vic 在本地生成新分析后，需要重新手动执行"复制+提交+推送"这几步，否则本地文件和 GitHub 仓库会不同步。**
   - Codex 如果没有访问 Vic 本地 `D:\` 盘的能力（大概率没有，Codex 通常跑在云端/沙盒），那么**GitHub 仓库里这份 HTML 就必须变成唯一真相源**，不能再假设有本地文件同步。这是一个需要 Vic 拍板的架构决定，不是技术问题。
6. **Git 提交身份**：本次提交用的是环境变量 `GIT_AUTHOR_NAME`/`GIT_AUTHOR_EMAIL` 传入 `kaguravicky-web` + 一个 noreply 占位邮箱，**没有写入全局 git config**（刻意避免，因为操作规范禁止改全局git config）。Codex 如果要继续提交，需要知道这个约定，或者请 Vic 提供真实提交身份。
7. **没有 `gh` CLI**：本机没装 GitHub CLI，本次创建分支/推送用的是裸 `git` 命令，PR 需要 Vic 自己手动点开链接创建（见本文档末尾）。如果 Codex 的运行环境里有 `gh` 或者能调用 GitHub API 创建 PR，可以让流程更自动化。
8. **凭证访问被安全策略拦截**：本次会话尝试用 `git credential fill` 读取已缓存的 GitHub 凭证去调 REST API（开 GitHub Pages）时，被 Claude Code 的自动模式分类器挡下——原因是这类命令会把明文凭证打印到工具输出/对话记录里。**这不是 bug，是刻意的安全设计**，Codex 如果有类似的沙盒策略，大概率也会挡同样的操作，需要请 Vic 手动操作那些需要凭证的一次性设置（比如开 Pages）。

---

## 4. 现有工作流程（从"财报出了"到归档完成）

1. **确认财报确实已发布**：目前**没有自动核实机制**。做法是直接对用户说的话/日期做网页搜索交叉验证（比如搜"XXX Q2 2026 earnings"，看返回结果的报道日期是否等于或早于"今天"）。**已知漏洞**：如果搜到的是"预期分析/earnings preview"文章而不是实际结果，容易搞错财报是否已发布——本次处理IBM时特意确认了这是"7/14的preliminary警示披露"而不是"7/22的正式财报"，这个区分是手动做的，没有工具强制检查。一个可能的改进方向是接入官方的公开财报日历API（例如 Nasdaq 的公开日历接口，免登录免key），但需注意**这类日历通常只覆盖正式排定的财报日期，抓不到像IBM这次这种"临时提前预警"的ad-hoc披露**——两种情况需要不同的核实方式，不能互相替代。
2. **确定对应财季和发布日期**：从搜索结果的新闻标题/日期直接读取，没有独立校验步骤。
3. **搜集资料**：调用 LongBridge MCP 拿实时价格（含盘前/盘中/盘后），然后多轮 WebSearch（本次GS+IBM两只股票总共做了约14次搜索）覆盖：EPS/营收实际vs预期、历史4季度数据、指引/管理层表态、价格反应及分析师目标价调整、内部人交易、同业read-through。**这一步的搜索轮次和覆盖面完全靠分析者临场判断，没有固定的"必须搜这几类"检查清单**（虽然系统提示词里补充规则那节列了几条"必须搜索"的类别，比如内部人交易）。
4. **交叉核验**：多篇报道数字不一致时（这次IBM的营收预期就有$17.86B和$17.9B两个版本），**没有统一的"以谁为准"规则写在系统提示词里**，我这次的操作惯例是：SEC官方文件 > 大型财经媒体（CNBC等）> 聚合类网站（Zacks式的数据聚合站）。
5. **计算评分**：见第1.2节，几乎全靠现场判断，只有"历史惊喜对比"和"持续性"和"成交量"这三项有可复现公式。
6. **形成"市场在告诉我们什么"**：这是纯分析写作，没有模板化的假设生成规则，靠分析者读完所有材料后自己提炼1-3条假设+对应验证信号。
7. **选择同业和行业基准**：靠常识判断（GS选了同日报告的JPM/WFC，IBM本来想找Accenture/HPE/Dell但没搜到同期数据，报告里坦承了这个信息缺口），**没有一个预设的"某行业对标哪几家"映射表**。
8. **生成验证点**：同上，纯分析写作。
9. **将报告加入网页**：手动用 Edit 工具在 `财报交易助手.html` 里 `RECORDS` 数组的 `/* __RECORDS_INSERT_POINT__ */` 标记下方插入一条 JS 对象字面量（不是严格JSON，用的是模板字符串，所以文本里的引号/换行不需要转义，但**要小心别漏改数组收尾的 `];`**——本次会话就因为一次 Edit 替换漏掉了收尾括号导致语法错误，靠 Node.js 跑一遍解析测试才抓出来）。完整字段结构见第4.4节。
10. **部署到 GitHub Pages**：**当前状态：Vic 已经被引导去手动开启（Settings → Pages → Deploy from branch → main → /root），但本次会话没有收到确认，不确定是否已完成。** 见第5节已知问题。
11. **补充 Vic 观点和复盘结果**：Vic 后续在对话里提出的看法，手动补进对应记录的 `vic_notes` 字段；交易/事件结果确认后，手动补进 `outcome` 字段。**没有提醒机制**——不会主动在T+2/T+3/T+5提醒 Vic 该回来复盘了，纯粹是 Vic 主动说了才补。

---

## 4.4 RECORDS 数据结构完整定义

在 `财报交易助手.html` 的 `<script>` 里：

```js
const RECORDS = [
/* __RECORDS_INSERT_POINT__ */
{
  type: "earnings" | "event",          // "earnings"=模式一财报分析，"event"=模式二事件产业链分析
  date: "YYYY-MM-DD",                  // 财报/事件发生日期
  ticker: "NVDA",                      // event型可用主题短名代替（无固定ticker时）
  company: "英伟达",                    // event型放事件名称
  score: 87,                           // 0-100，event型省略此字段（不写，不是写null）
  rec: "resonance"|"doubt"|"repricing"|"deterioration",  // 四象限判定，event型省略
  dims: {quality:32, marketread:26, fundamentals:19},     // 三维度分解，event型省略；上限分别是40/35/25
  report: `完整报告文本，用模板字符串反引号包裹，可以直接换行不用转义`,
  vic_notes: "",                       // Vic后续论点，默认空字符串
  outcome: ""                          // 复盘结果，默认空字符串，以"-"或"亏"或"止损"开头会在页面上显示红色
},
];  // ⚠️ 别忘了这个收尾括号——插入新记录时最容易漏掉
```

**新增记录的正确方法**：见 [`EXAMPLES.md`](EXAMPLES.md) 第1节，有完整可复制的操作步骤。
**补充 Vic 观点/复盘结果的正确方法**：见 [`EXAMPLES.md`](EXAMPLES.md) 第2节。

**为什么用 `财报交易助手.html` 而不是 `index.html`：没有技术原因，纯粹是历史遗留——文件在本地生成时就叫这个名字，上传到仓库时原样保留了文件名。** 我在对话里提议过改名成 `index.html`（这样开 GitHub Pages 后根地址不用带文件名），但 Vic 还没有回复确认要不要改，**这是一个待 Vic 拍板的事项，不是技术限制**。

---

## 5. 已知问题与待办事项

完整清单见 [`TODO.md`](TODO.md)。这里只列**建议 Codex 优先处理的前三项**：

1. **打通实时数据的确定性替代方案**：落地 `longport` Python SDK 直连方案（见第3节第1、2条），同时解决"实时价格"和"历史K线/成交量均值"两个问题——后者目前完全没有真实数据，全靠新闻里的定性描述。
2. **把本地 HTML 归档文件和 GitHub 仓库的同步流程自动化**，或者干脆确定"仓库是唯一真相源"并停止依赖任何本地拷贝——目前这是一个手动、容易遗忘的步骤（见第4.4节文件位置说明）。
3. **补上"财报是否真的已发布"的核验步骤**，哪怕只是要求分析开始前先做一次专门的"这是正式财报还是预告/传闻/预期分析"确认性搜索，减小误判风险（IBM这次的"7/14提前预警 vs 7/22正式财报"这种边界情况以后还会再出现）。

---

## 6. 历史决策和用户偏好

- **Vic 的报告偏好**：结构清晰、分维度打分、每项都要有具体数字支撑（不能空泛定性）、结尾必须给"下一个验证点"（可验证、可证伪，不能是"继续观察"这种空话）。语气要直接、不绕弯子，能明确说"这个数据缺失/没查到"就直接说，不要为了显得完整而编数据。
- **明确不希望出现的内容（2026-07-08转向后）**：不给买卖建议、不给入场点、不给止损位、不给仓位/Peel减仓计划——这是本次转向里最核心的一条边界，**Codex 接手后如果被要求"顺便给个操作建议"，应该提醒这条边界已经被 Vic 明确划掉，不是遗漏**。
- **为什么从"交易助手"转向"基本面深度分析助手"**：Vic 原话大意是"财报是滞后数据，市场的解读是评分很重要的一方面，因为永远有人想到了我们没想到的，知道了我们不知道的"——核心逻辑是：与其用财报数字直接推导买卖信号（滞后信息，别人也看得到），不如把分析重心放在"数字和市场反应是否一致"上，一致时确认叙事，不一致时倒推市场可能知道什么我们不知道的东西。这是一个从"我来做交易判断"到"我来帮你理解市场在想什么"的定位转变。
- **哪些分析结论曾被 Vic 修正**：本次交接时点为止，Vic 还没有对 GS/IBM 这两份具体分析给出修正意见（`vic_notes` 字段目前都是空的）。
- **重要设计决策**：
  - 用"四象限"（共振确认/市场存疑/预期重定价/恶化确认）代替"四档买卖建议"，是这次转向的核心设计
  - 归档形式选择"静态HTML+手动Edit插入记录"而不是数据库/CMS，是 Vic 的偏好
  - Markdown报告文件被明确否决、换成HTML（"我要上传这个html，不是md文件"）——**说明 Vic 更看重"网页归档能持续浏览"，而不是"单次可读的文档"**，这是一个值得记住的产品方向信号
- **对分析质量的一般期待**：从这次交互看，Vic 希望每次分析都是基于实际主动查证的资料（多来源交叉核对），而不是基于"恰好搜到的一两条信息"就下结论；数据缺失时要如实标注"待确认/查不到"，不要为了显得完整而编造。Codex 接手后建议默认按这个标准执行，而不是等被指出不足才补。

---

## 7. 最后一段：写给 Codex 的坦白话

**这个助手目前实际上能做什么：** 能在 Vic 一句话提到某只股票或某个事件后，通过 LongBridge 实时报价 + 多轮网页搜索，产出一份结构清晰、有具体数字支撑、明确指出"市场反应和数字是否一致"的深度分析报告，并归档进一个可搜索、可筛选的静态网页。这套流程做过一次实战验证（本次会话，GS + IBM，2026-07-14），产出质量我认为是合格的，但样本量只有2份，还没有经过 Vic 大量使用后的反馈打磨。

**哪些步骤仍然依赖人工：** 几乎所有"打分"步骤依赖分析者（LLM）当场的主观判断，只有"历史惊喜对比""持续性""成交量"三个子项有可复现公式；本地HTML文件到GitHub仓库的同步是手动的；GitHub Pages的开启需要 Vic 自己在设置页面点；PR 的创建需要 Vic 自己点链接确认（本次交接分支已推送，PR 没有人创建，见下方链接）；"财报是否真的已发布"没有自动核验。

**Codex 接手时最容易踩的坑：** (1) 沿用旧的v1交易框架（Peel减仓、止损位、🚀立即追这些），Vic已经明确划掉了这条线，见 `prompts/系统提示词_v1_交易信号版_已废弃.md` 上方的废弃说明；(2) 把评分体系当成一个确定性算法来信任——它更像一份"评分指南"而不是"评分公式"，大部分子项需要现场判断，如果 Codex 想做成真正确定性的评分工具，需要先补齐大量没写清楚的分档规则（并且很可能需要 Vic 认可这些新补的规则）；(3) 忘记本地文件和仓库文件之间没有自动同步，改了一边以为另一边也更新了。

**有没有任何只有我知道、但上述清单没有覆盖的重要背景：** 没有额外的、这份文档之外才知道的隐藏背景——本文档已经把这次会话里能确认、且适合公开在这个仓库里的内容都写进去了。如果后续发现本文档遗漏了什么，建议直接问 Vic 补充，而不是假设存在只有某次对话才知道的信息。

---

## 附：仓库当前维护状态

- **默认分支：** `main`，最后一次提交 `8d807e1`（"Replace md report with earnings assistant HTML archive"，2026-07-15）
- **main 分支内容：** 仅 `财报交易助手.html` 一个文件
- **本交接分支：** `handover/codex-integration`（本次新建，包含本文档 + `prompts/` + `DATA_SOURCES.md` + `TODO.md` + `EXAMPLES.md` + `FILES.md`）
- **未提交改动：** 无（本地临时clone工作区在推送后是干净的）
- **其他分支：** 无（除本次新建的这个）
- **GitHub Pages：** 状态未知——本次会话已指导 Vic 手动开启，未收到确认，见 `TODO.md`
