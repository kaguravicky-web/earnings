# 已知问题与待办事项

## 已知 Bug

1. **`index.html` 的编辑操作容易漏改数组收尾 `];`**——曾发生过替换 `RECORDS` 数组时遗漏收尾括号、导致页面脚本无法运行的问题。以后每次修改记录后，都必须运行语法校验并检查记录数量，再提交变更。
2. **成交量均量倍数目前完全没有真实数据**——见 `DATA_SOURCES.md` 第5条，`quote-history-candlesticks` MCP工具确认损坏，所有分析报告里这一项目前都是定性描述，不是真实计算值。

## GitHub Pages 访问或部署问题

- GitHub Pages 已启用，旧中文文件名的直达地址可访问。
- 本 PR 将页面统一改为 `index.html`。合并并完成 Pages 部署后，应验证项目根地址返回 200 且页面功能正常。

## 数据获取不稳定的环节

见 `DATA_SOURCES.md`，重点是：
- 成交量/历史均量（完全无真实数据源，见上）
- 期权 IV/Put-Call（完全没有解决方案，每次都要 Vic 手动提供）
- 机构资金/暗池数据（完全没有解决方案）
- 历史4季度EPS数据有时会搜到"污染"数字（和其他季度不在一个量级），需要人工用常识甄别

## 容易出现事实错误或日期错误的环节

- **"财报是否真的已发布"没有自动核验机制**，容易把"预期分析/preview文章"误当成"实际结果"，本项目至今没有真实出过这个问题，但机制上的漏洞是存在的，风险仍在。
- **"提前预警/preliminary披露" vs "正式财报"的区分需要额外小心**——IBM这次 7/14 发布的是抢跑警示，正式电话会和完整指引更新在 7/22，如果不注意这个区分，容易把"指引"相关的评分子项算错（本次是手动注意到并在报告里专门标注了这个区别）。

## 尚未实现的功能

1. 没有任何自动化/定时任务——纯对话触发，无 cron，无扫描器
2. 没有"T+2/T+3/T+5复盘"的结构化模板/提醒机制，见 `HANDOVER.md` 第1.1节
3. 没有把评分规则里明确的部分（历史惊喜对比、持续性、成交量三项）抽出来做成一个独立可复用的计算脚本——**这件事此前其实做过一次！** 2026-07-06 到 2026-07-08 之间，本项目曾经有一版"填表单+自动打分"的 HTML 计算器（JS 里实现了完整的 EPS/营收/指引/历史趋势/价格反应/技术结构等打分逻辑，逻辑经过 Node.js 单测验证），但后来 Vic 明确要求"不填表单，你来分析"，那个版本被整个替换成纯归档页，**计算逻辑代码目前已经不存在于任何地方**（没有保留在仓库或本地文件里，只存在于被覆盖之前的对话历史中）。如果 Codex 想要一个确定性打分脚本，需要参照 `HANDOVER.md` 第1.2节里写的规则重新实现，而不是去找那份旧代码——它已经不在了。

## 已经讨论但没有完成的改进

1. HTML 文件改名为 `index.html`（本 PR 已处理，待合并后验证）
2. GitHub 仓库已确定为唯一真相源；后续通过分支和 PR 更新，不同步仓库外副本

## 建议 Codex 优先处理的前三项工作

1. 打通 `longport` Python SDK 直连方案（价格 + 历史K线/成交量），见 `HANDOVER.md` 第3节第1、2条
2. 确定本地文件 vs GitHub 仓库的同步机制/唯一真相源，消除手动同步的遗忘风险
3. 补上"财报是否真的已发布/是否为预期分析而非实际结果"的核验步骤，即使只是要求分析开始前先做一次专门确认
